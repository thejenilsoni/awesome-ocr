# Marker

Marker is a fast, high-accuracy tool for converting PDFs to Markdown. It handles complex layouts, removes artifacts like headers/footers, and formats tables and equations.

## Installation

```bash
pip install -r requirements.txt
```

## Usage

```bash
python marker_ocr.py <path_to_pdf>
```
