# Nougat (Neural Optical Understanding for Academic Documents)

Nougat is a Visual Transformer model from Meta AI that transcribes scientific PDFs into Markdown, specifically handling LaTeX math and tables.

## Installation

```bash
pip install -r requirements.txt
```

## Usage

```bash
python nougat_ocr.py <path_to_pdf>
```
