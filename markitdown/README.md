# MarkItDown

MarkItDown is a lightweight Python utility from Microsoft for converting various files (PDF, Word, Excel, Images) to Markdown. It uses OCR for images and PDFs.

## Installation

```bash
pip install -r requirements.txt
```

## Usage

```bash
python markitdown_ocr.py <path_to_file>
```
