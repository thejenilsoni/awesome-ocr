# MinerU (Magic-PDF)

MinerU is a high-quality tool for converting PDF documents into machine-readable formats (e.g., Markdown, JSON). It excels at handling complex layouts, formulas, and tables.

## Installation

```bash
pip install -r requirements.txt
```

## Usage

```bash
python mineru_ocr.py <path_to_pdf>
```
