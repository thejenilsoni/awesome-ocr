# EasyOCR

EasyOCR is a Python library for Optical Character Recognition (OCR) that is both easy to use and provides high accuracy. It supports 80+ languages and is built on top of PyTorch.

## Installation

```bash
pip install -r requirements.txt
```

## Usage

```bash
python easyocr_ocr.py <path_to_image>
```
